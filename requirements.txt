# Versões fixadas: sem pin, o pip pode resolver versões antigas e incompatíveis
# de pacotes transitivos (o caso clássico é o s3transfer/boto3).
# Resolvidas com `pip install --dry-run` em 2026-09-23.

boto3==1.43.100
botocore==1.43.100
s3transfer==0.19.2

fastf1==3.8.3
pandas==2.3.3
pyarrow==25.0.1
requests==2.34.2
python-dotenv==1.2.3
tqdm==4.70.1
